spring:
  datasource:
    url: jdbc:mysql://localhost:3333/lift_craft
    username: root
    password: admin
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: none

  sql:
    init:
      mode: always
